
let fruits = ["banana", "apple", "orange", "mango"];

// fruits = fruits.sort(); // order by alphabetic order
// fruits = fruits.sort().reverse(); // reverted alphabetic order

for(let fruit of fruits) {
    console.log(fruit);
}
